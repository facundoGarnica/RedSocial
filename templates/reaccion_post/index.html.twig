{% extends 'base.html.twig' %}

{% block title %}ReaccionPost index{% endblock %}

{% block body %}
    <h1>ReaccionPost index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Emoticon</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reaccion_post in reaccion_posts %}
            <tr>
                <td>{{ reaccion_post.id }}</td>
                <td>{{ reaccion_post.emoticon }}</td>
                <td>
                    <a href="{{ path('app_reaccion_post_show', {'id': reaccion_post.id}) }}">show</a>
                    <a href="{{ path('app_reaccion_post_edit', {'id': reaccion_post.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reaccion_post_new') }}">Create new</a>
{% endblock %}

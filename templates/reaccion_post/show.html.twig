{% extends 'base.html.twig' %}

{% block title %}ReaccionPost{% endblock %}

{% block body %}
    <h1>ReaccionPost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reaccion_post.id }}</td>
            </tr>
            <tr>
                <th>Emoticon</th>
                <td>{{ reaccion_post.emoticon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reaccion_post_index') }}">back to list</a>

    <a href="{{ path('app_reaccion_post_edit', {'id': reaccion_post.id}) }}">edit</a>

    {{ include('reaccion_post/_delete_form.html.twig') }}
{% endblock %}

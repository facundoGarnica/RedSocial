<form method="post" action="{{ path('app_reaccion_post_delete', {'id': reaccion_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reaccion_post.id) }}">
    <button class="btn">Delete</button>
</form>

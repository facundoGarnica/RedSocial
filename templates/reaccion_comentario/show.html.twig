{% extends 'base.html.twig' %}

{% block title %}ReaccionComentario{% endblock %}

{% block body %}
    <h1>ReaccionComentario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reaccion_comentario.id }}</td>
            </tr>
            <tr>
                <th>Emoticon</th>
                <td>{{ reaccion_comentario.emoticon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reaccion_comentario_index') }}">back to list</a>

    <a href="{{ path('app_reaccion_comentario_edit', {'id': reaccion_comentario.id}) }}">edit</a>

    {{ include('reaccion_comentario/_delete_form.html.twig') }}
{% endblock %}
